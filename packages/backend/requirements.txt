# TrueHire Backend Requirements
# Python dependencies for FastAPI backend

# Web framework
fastapi
uvicorn[standard]
pydantic

# Testing
pytest

# Machine Learning
scikit-learn
joblib
pandas
numpy

# File handling
python-multipart

# PDF parsing for resume extraction
pdfplumber
pypdf

# DOCX parsing for resume extraction
python-docx

# HTTP client for external APIs
httpx

# Environment variables
python-dotenv

# Fuzzy string matching for company verification
rapidfuzz

# HTML parsing for URL scraping
beautifulsoup4
lxml

# PostgreSQL database driver
psycopg2-binary

# Semantic embeddings (Gemini only — no local models needed on Cloud Run)
google-genai

# JWT verification for Clerk auth
# Note: pyjwt is the package name, but imports as 'jwt'
pyjwt[crypto]
