{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:py": "cd packages/backend && .venv\\Scripts\\activate && python -m uvicorn main:app --reload --port 8000"
  }
}
