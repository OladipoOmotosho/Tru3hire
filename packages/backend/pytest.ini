[pytest]
# Tests import `from app...`, so pytest must root at packages/backend.
testpaths = tests
asyncio_mode = strict
markers =
    external: tests that hit live external services (Gemini/Adzuna). Excluded in CI via -m "not external".
